31/5/25

Currently implementation is verified with 1 grp_vars
2+ grp_vars is untested but should work

update summarizer to work with model.ds instead of model.data 
update HiBRC interface to work with model.ds, using attrs to keep track of grp_vars
update HiBRCfine notebook to new workflow

add new file sample_prop.csv, the form of data to be used

Major change to _dataloader
    fix N 
    add grp_vars_part to final ds

    For duplicated grp_vars, keep participants side

    added validate_pop_proportions
        ensure the proportions sum to 1
    added warnings and guides to pop_proportions

    The implementation assumes a quadruple format of input

        a DataFrame containing data, the column name for pop age, the column name for category, the column name for population proportions
        For example, a valid input quadruple for the pandas dataframe `data` below would be [data, "age", "category", "proportions"]
        sample = pd.DataFrame({'age':[1,2,1,2],
            'category':['A', 'A', 'B', 'B'], 
            'proportions':[0.2, 0.3, 0.8, 0.7]})


Maybe clean up the __init__ function, currently it is chunky